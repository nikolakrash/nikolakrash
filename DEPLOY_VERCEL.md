# Инструкция по загрузке сайта на Vercel

## Вариант 1: Через веб-интерфейс (самый простой)

### Шаг 1: Регистрация на Vercel

1. Зайдите на https://vercel.com
2. Нажмите **Sign Up**
3. Войдите через GitHub, GitLab или Email

### Шаг 2: Создать проект

1. В панели Vercel нажмите **Add New...** → **Project**
2. Выберите **Import Git Repository** (если есть репозиторий) или **Deploy** (для загрузки файлов)

### Шаг 3: Загрузить файлы (если нет Git)

1. Нажмите **Deploy** → **Browse**
2. Выберите папку `/Users/macbook/teleboosting-sait`
3. Или перетащите папку в браузер
4. Нажмите **Deploy**

### Шаг 4: Настроить домен

1. После деплоя перейдите в **Settings** → **Domains**
2. Добавьте домен: `teleboosting.com`
3. Добавьте поддомен: `www.teleboosting.com`
4. Vercel покажет DNS записи для настройки

### Шаг 5: Настроить DNS в Cloudflare

1. Зайдите в панель Cloudflare: https://dash.cloudflare.com
2. Выберите домен `teleboosting.com`
3. Перейдите в раздел **DNS**
4. Добавьте записи, которые показал Vercel:

   **CNAME запись:**
   - **Name:** `@` (или `teleboosting.com`)
   - **Target:** `cname.vercel-dns.com` (или то, что показал Vercel)
   - **Proxy status:** 🟠 Proxied (включен)
   - **TTL:** Auto

   **CNAME запись для www:**
   - **Name:** `www`
   - **Target:** `cname.vercel-dns.com`
   - **Proxy status:** 🟠 Proxied
   - **TTL:** Auto

5. Подождите 5-10 минут для распространения DNS

---

## Вариант 2: Через Git (рекомендуется)

### Шаг 1: Создать Git репозиторий

```bash
cd /Users/macbook/teleboosting-sait

# Инициализировать Git (если еще не сделано)
git init

# Добавить все файлы
git add .

# Создать первый коммит
git commit -m "Initial commit"
```

### Шаг 2: Загрузить на GitHub/GitLab

1. Создайте репозиторий на GitHub (https://github.com/new)
2. Название: `teleboosting-site` (или любое другое)
3. Сделайте репозиторий **Public** или **Private** (оба работают)
4. Выполните команды, которые покажет GitHub:

```bash
git remote add origin https://github.com/ВАШ_USERNAME/teleboosting-site.git
git branch -M main
git push -u origin main
```

### Шаг 3: Подключить к Vercel

1. Зайдите на https://vercel.com
2. Нажмите **Add New...** → **Project**
3. Нажмите **Import Git Repository**
4. Выберите ваш репозиторий `teleboosting-site`
5. Нажмите **Import**

### Шаг 4: Настроить проект

1. **Framework Preset:** `Other` (или оставьте автоопределение)
2. **Root Directory:** `./` (оставьте по умолчанию)
3. **Build Command:** оставьте пустым (для статики не нужен)
4. **Output Directory:** оставьте пустым
5. Нажмите **Deploy**

### Шаг 5: Настроить домен

1. После деплоя перейдите в **Settings** → **Domains**
2. Добавьте домен: `teleboosting.com`
3. Добавьте поддомен: `www.teleboosting.com`
4. Настройте DNS в Cloudflare (см. Вариант 1, Шаг 5)

---

## Вариант 3: Через Vercel CLI (для продвинутых)

### Шаг 1: Установить Vercel CLI

```bash
npm install -g vercel
```

### Шаг 2: Войти в Vercel

```bash
cd /Users/macbook/teleboosting-sait
vercel login
```

### Шаг 3: Задеплоить

```bash
vercel
```

Следуйте инструкциям:
- Set up and deploy? **Y**
- Which scope? выберите ваш аккаунт
- Link to existing project? **N**
- Project name? `teleboosting-site`
- Directory? `./`
- Override settings? **N**

### Шаг 4: Добавить домен

```bash
vercel domains add teleboosting.com
vercel domains add www.teleboosting.com
```

Затем настройте DNS в Cloudflare.

---

## Настройка для production

### Добавить файл `vercel.json` (опционально)

Создайте файл `/Users/macbook/teleboosting-sait/vercel.json`:

```json
{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/static/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ]
}
```

Это обеспечит:
- Правильную работу роутинга
- Кеширование статических файлов

---

## Обновление сайта

### Если используете Git:

```bash
cd /Users/macbook/teleboosting-sait
git add .
git commit -m "Update site"
git push
```

Vercel автоматически задеплоит изменения!

### Если загружали через веб-интерфейс:

1. Зайдите в проект на Vercel
2. Перейдите в **Deployments**
3. Нажмите **Redeploy** или загрузите файлы заново

---

## Проверка работы

1. После деплоя Vercel даст вам URL: `https://teleboosting-site.vercel.app`
2. Проверьте, что сайт открывается
3. После настройки DNS проверьте: `https://teleboosting.com`

---

## Преимущества Vercel

✅ **Бесплатно** для личных проектов  
✅ **Автоматический SSL** для всех доменов  
✅ **Быстрый CDN** по всему миру  
✅ **Автодеплой** при изменениях в Git  
✅ **Preview deployments** для каждой ветки  
✅ **Аналитика** (опционально)  
✅ **Простота** - никакой настройки сервера  

---

## Решение проблем

### Сайт не открывается

1. Проверьте статус деплоя в панели Vercel
2. Проверьте логи: **Deployments** → выберите деплой → **View Function Logs**
3. Проверьте DNS: `nslookup teleboosting.com`

### DNS не работает

1. Убедитесь, что в Cloudflare записи правильные
2. Проверьте, что Proxy включен (🟠 Proxied)
3. Подождите 10-15 минут для распространения DNS

### Файлы не загружаются

1. Проверьте размер файлов (бесплатный план: до 100MB)
2. Убедитесь, что все файлы в папке проекта
3. Проверьте, что нет скрытых файлов, которые мешают

---

## Полезные ссылки

- Vercel Dashboard: https://vercel.com/dashboard
- Документация: https://vercel.com/docs
- Поддержка: https://vercel.com/support
